id: js.xss
message: "사용자 입력이 정제 없이 HTML 출력으로 흘러 들어갑니다 (XSS)."
severity: high
cwe: CWE-79
owasp: "A03:2021-Injection"
languages: [javascript, typescript]
sources:
  - {pattern: member, object: req, property: [query, body, params, headers, cookies]}
  - {pattern: member, object: location, property: [hash, search, href]}
sinks:
  - {pattern: call, callee: [res.send, res.write, res.end, document.write, element.insertAdjacentHTML], arg: 0}
sanitizers:
  # HTML 이스케이프
  - {callee: [escapeHtml, sanitizeHtml, DOMPurify.sanitize, encodeURIComponent, encodeURI, he.encode, validator.escape, xss, escape-html]}
  # 수치 변환 — 숫자로 바뀐 값은 payload 를 담지 못한다
  - {callee: [parseInt, parseFloat, Number, Number.parseInt, Number.parseFloat, BigInt, Math.floor, Math.round, Math.abs, toFixed]}
  # 형식 검사 가드 — if (/^[0-9]+$/.test(x)) { ... } 를 인정한다
  - {callee: [test, validator.isInt, validator.isNumeric, validator.isAlphanumeric, validator.isUUID, isNaN, Number.isInteger, Number.isFinite]}
