# 작업 로그 — 2026-09-18

## GUI 스캔 파싱 병렬 기본값 (1 → auto)

웹/데스크톱 GUI 스캔이 항상 단일 프로세스로 파싱하고 있었다. `views.py` 의 스캔 잡이
`CPGUARD_JOBS` 환경변수로만 워커 수를 받고 기본값이 `"1"` 이라, GUI 에는 워커 입력칸이
없으니 사실상 병렬을 켤 방법이 없었다(env 를 알고 미리 박아야만 했다).

기본값을 `min(os.cpu_count() or 1, 8)` 로 바꿨다. env 로 여전히 오버라이드 가능
(`CPGUARD_JOBS=N`). GUI 입력칸은 만들지 않았다 — 좋은 auto 기본값이면 대부분 유저는
env 도 필드도 안 건드리고, 튜닝 수요가 실제로 생기면 그때 넣는다.

- 8 캡: `scan_path` 의 jobs 는 **파싱 단계만** 병렬화한다(taint/함수요약 분석은 순차).
  코어를 그 이상 줘도 파싱 이득은 프로세스 스폰 오버헤드에 먹힌다.
- frozen exe 안전: 진입점에 `multiprocessing.freeze_support()` 이미 적용돼 있다
  (scanner.py docstring 명시).

변경: `cpguard/web/views.py:249`.

## 벤치마크 러너 성능 메모 (코드 변경 아님)

자바 OWASP 벤치가 느린 원인을 확인했다. `bench/owasp_benchmark.py` 기본이
`--mode project` + `--workers` 미지정 → `_scan_project` 가 `scan_path(jobs=1)` 로
단일 프로세스. project 모드는 전체 코퍼스에 파일간 함수요약을 붙여 가장 무거운 조합이다.

- 최소 조치: `--workers 8`. project 모드도 workers 를 `scan_path(jobs=)` 로 넘긴다
  (파싱만 병렬이라 sub-linear).
- 큰 레버: `--mode file` (파일 단위 ProcessPoolExecutor, 전 단계 병렬). 단 OWASP Java 는
  `org.owasp.benchmark.helpers.*` 를 거치는 흐름이 있어 파일 모드면 재현율이 떨어질 수
  있다 → `--limit 300` 으로 project vs file 재현율 먼저 비교하고 결정.
- 개발 루프에선 `--limit` 로 빠르게, 전량은 수치 기록할 때만.

## C# CWE-89 재현율 대개편 (12.7% → 79.5%)

C# 전체 점수(0.245)의 발목이던 CWE-89(코퍼스 64%, 재현율 12.7%)를 재현으로 근본원인
두 개를 특정해 고쳤다. SARD C# 스위트를 최소재현으로 프로브(b1~b3·r1~r3)해 격리했다.

**버그 B — `cmd.CommandText = <오염>` 대입 싱크 미인식 (커밋 1b96158)**
- 스위트는 전부 `CreateCommand → cmd.CommandText = query → cmd.Execute*()` 체인.
  규칙이 CommandText 를 call 싱크로만 둬서 대입 형태를 통째로 놓쳤다.
- `_check_sinks` 는 호출식만 검사한다(engine.py:722). 대입문(`ir.Assign`) 핸들러는
  RHS 만 싱크 검사하고 target 은 안 봤다.
- 수정: `SinkPattern` 에 `kind='assign'` + `property` 추가, `_check_assign_sink` 로
  `obj.Prop = 오염` 검사. 상수 CommandText(파라미터라이즈드)는 RHS 가 안 오염돼
  안 걸린다 → 정밀도 유지.

**버그 A — `Main(string[] args)` 명령행 인자 소스 미인식 (커밋 b2b5c25)**
- 코퍼스 CWE-89 의 1/4 이 `args[i]` 소스. 소스 목록에 없어 통째 미탐.
- `args[i]` 는 path_of 가 `'args.i'` 를 준다 → name 소스 `args` 가
  `startswith('args.')` 로 잡는다. 엔진 수정 없이 규칙 1줄.
- ponytail: 1줄 먼저 넣고 정밀도를 측정해 무너지지 않는 것을 확인한 뒤 채택
  (Main 게이트 같은 방어는 필요 없었다). 안전(prepared) 버킷 전 소스 0% = FP 없음.

**측정 (정본 manifest state 라벨, CWE-89 랜덤 1500 표본)**
- 재현율 12.7% → **79.5%**, 정밀도 87.8→89.3%, 오탐률 2.4→15.3%, 점수 0.103 → **0.642**
- 구성별: args 0→78~86%, readline 0→78~80%, shell 0→68~74%, hardcoded 0%(상수라
  정상 미탐), prepared(안전) 전 소스 0%(FP 없음).
- CWE-89 가 C# 코퍼스 64% → 전체 C# 점수 0.245 → ~0.5 대 예상.
- 회귀: 전체 유닛 529 통과(MCP 5건 실패는 기존 mcp 모듈 문제, 무관).

**남은 것**
- 이 표본 수치는 강한 추정. **bench/README.md 표와 bench 결과 json 은 전체 33k 공식
  실행으로 갱신해야 정본이다**(아직 안 함 — 벤치 러너가 C# 를 project 모드로 전체
  트리 스캔해 매우 느림). 대외 문서 4종(README·README.ko·promo-kit·commercialization)의
  C# 수치도 그때 같이.
- 버그 A 의 `args` 소스는 csharp.sqli 에만 넣었다. 다른 C# 규칙(path/cmd/ldap/xpath)도
  같은 args 소스 공백이 있을 수 있다 — 별건.

## v0.2.0 데이터 흐름 단계 100배 느려진 원인 (속도 회귀, 탐지는 개선)

사용자 보고: "2.0 이 1.4 보다 진단 성능이 매우 낮아졌다, 특히 데이터 플로우 추적."
두 버전을 같은 입력(OWASP Java 200파일, jobs=1)으로 재서 갈랐다 — **탐지는 오히려
개선(201→217건, 불확실 78→10)이고 dataflow 단계만 0.8s → 79.4s.**

**증거 순서**
- cProfile: 284s 중 275s 가 `compute_summaries → _summarize`. `_summarize` 호출
  v0.1.4 354회 → 현재 53,868회(152배).
- git bisect(v0.1.4..v0.2.0, java200 25s 임계): 첫 악화 = `53ab4a3` "모듈 경로로
  부른 사용자 함수를 찾지 못하고 있었다" (3s → 36s).
- 레지스트리 직접 측정: v0.1.4 는 200파일에서 **함수 3개**(`doGet`·`doPost`·
  `getNextNumber` — 맨 이름 키라 200개 파일의 동명 메서드가 서로 덮어씀). 현재는
  1,613키/**402함수**. **v0.1.4 가 빨랐던 것은 분석을 안 했기 때문이다.**
- 규칙별 요약 시간 측정: Java 규칙은 10개인데 `compute_summaries` 가 59회.
  `active`(scanner.py:380)가 싱크 리터럴 텍스트 존재만 보고 언어를 안 봐서 Java 의
  `exec`·`query`·`write` 가 cpp·php·js·kotlin·swift 규칙을 깨웠다. 파일별
  `applicable` 은 언어로 거르므로 **비Java 49개 규칙의 요약은 계산만 하고 한 번도
  안 쓰였다 — 101.6s 중 87.8s(86%).** `14ea804`(perf 커밋) 부터 있던 잠재 결함이
  레지스트리 3→402 로 402배 커져 드러났다.
- 가설 검증: Java 규칙 10개만 넘겨 스캔 → 82.7s → 17.4s, findings·불확실·지문 동일.

**수정 (커밋 15991cd)** — `active` 에 파싱된 파일 언어와의 교집합 조건 1줄.
java200 · jobs=1: **83.6s → 18.3s**(dataflow 79.4 → 15.2), 결과 동일, 회귀 530 통과.
테스트 `tests/test_summary_rule_filter.py`: `compute_summaries` 스파이로 "존재 언어의
규칙만 호출" 을 지킨다(탐지 자체가 남아 있는지도 같이 — 음성 항목 없는 테스트 금지).

**교훈**: 빨라진 것과 맞아진 것을 같은 눈금으로 보지 말 것. v0.1.4 의 2초는 정확도의
결과가 아니라 결함(이름 충돌)의 결과였다. 성능 비교는 반드시 **결과 지문이 같은지**를
같이 찍는다. 그리고 최적화 필터(싱크 리터럴)는 그 뒤 소비자(언어 필터)와 같은 조건을
가져야 한다 — 앞 단계가 더 느슨하면 그 차이만큼이 순수 낭비다.

**남은 것 — 2차(요약 단가 2배)**: Java 규칙만으로 53ab4a3 8.7s → HEAD 17.5s.
탐지 201→217 의 대가와 낭비가 섞여 있다. 53ab4a3..HEAD 엔진 커밋 전부를 java 규칙만
으로 스윕해 계단을 찾는 중(아래에 이어 적음).

**2차 스윕 결과(53ab4a3..HEAD 엔진 커밋 24개, java 규칙만, jobs=1)** — 단일 계단 없이
누적: 8.9 → 9.7(3ac51b6, 탐지 201→225) → 11.8(b7699e7) → 13.3(2d222cd) → 14.5(f792a90)
→ 15.2(301ec99) → 14.0 → **17.3(a185caf, +24%, Java 결과 변화 0)** → 17.4. 다른 계단은
각각 타 언어 점수를 움직인 정당한 분석이라 손대지 않았다.

**a185caf 만 낭비(커밋 = perf: _defaulted_path)**: `_defaulted_path` 가 else 없는 모든
if 에서 요약 실행마다 조건 워커를 돌려 같은 노드를 128.7배 재계산(552노드 → 71,062회).
Java 에서 이 판정이 맞은 적 0회. ①대입변수∩env 비면 즉시 None ②조건 경로를 if 노드에
규칙별 메모 → 걷기 6,340회. 17.8 → 15.6s, sha 동일, 530 통과.

**측정 함정 두 개(다음에 또 속지 말 것)**
- **cProfile 은 잔호출 함수를 과대 계상한다.** 프로파일이 조건 워커에 8.7s 를 매겼는데
  실제 벽시계 절감은 2.2s 였다. 계산이 큰 곳은 프로파일로 찾되, **절감 예측은 저오버헤드
  벽시계 래퍼**(perf_counter 누적)로 다시 잰다. 지금 `_defaulted_path` 진짜 지분 4.8%.
- **동시 실행은 타이밍을 오염시킨다.** bisect 를 cProfile 과 같이 돌려 36~48s 구간이
  노이즈였고, 첫 재타이밍도 회귀 스위트와 겹쳐 1s 차이가 났다. good/bad 이분엔 문제
  없었지만 계단 크기는 순차 재측정으로만 믿는다.

**최종(java200 · jobs=1 · 전체 규칙)**: v0.1.4 5.0s(틀린 분석) → 보고 시점 83.6s →
**15.6s**. 결과(217건·불확실 10) 불변. 남은 ~15s 는 402함수 × 10규칙 × ~2.3반복의
정당한 프로시저간 분석 — `_check_sinks` 3.8s·`_apply_mutations` 2.9s·`_run_nested`
1.3s 로 분산, 단일 낭비 없음.

**구조적 다음 단계(안 함, 필요해지면)**: 요약이 규칙마다 통째로 재계산된다. Summary 의
returns_tainted·taints_self·self_returns·arity 는 규칙과 무관하고 sink_paths·source_trace
만 규칙 의존이라, 전자를 한 번 계산해 공유하면 규칙 수만큼의 배수를 더 줄일 수 있다.
그리고 `_check_sinks` 의 `_iter_calls` 가 문마다 트리를 다시 걷는다(11.4M 호출) — 문
단위 호출 목록 캐시가 다음 후보.
