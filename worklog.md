# CPGuard 현황 브리핑 — SPARROW 기능 비교 요청용

이 문서는 **다른 환경의 AI에게 넘기는 인수인계 문서**다. 목적은 하나다:
상용 SAST(SPARROW 5.6)를 브라우저로 확인한 뒤, **CPGuard 에 붙일 만한 기능 목록**을 받는 것.

문서 하나로 판단이 서도록 프로젝트 정체·현재 구현·측정치·한계·제약을 모두 담았다.
마지막 절에 **요청하는 산출물 형식**이 있다.

---

## 1. CPGuard 는 무엇인가

오픈소스 정적 보안 분석기. 졸업작품에서 출발해 계속 개발 중이고, 만든 사람은 **현업 취약점
진단원(화이트해커)** 이다. 그래서 도구가 아니라 **진단 업무 전체**를 대상으로 한다 —
탐지에서 끝나지 않고 정오탐 판정, 발주처 제출 산출물까지 이어진다.

- 저장소: https://github.com/KimJeju/cpguard (Apache-2.0)
- 현재 버전 0.1.4, Windows 단일 설치본(49MB, 파이썬·인터넷·관리자 권한 불필요)
- 스택: Python 3.11+ · tree-sitter · Django(SSR) · SQLite(데스크톱) / PostgreSQL(서버 예정)
- **완전 오프라인 동작**이 핵심 제약. 선택적 LLM 트리아지만 외부 통신.
- 주 시장: 한국 취약점 진단 실무(주요정보통신기반시설·전자금융·공공 SI). 발주처가 받는 건
  취약점 목록이 아니라 **점검표와 보고서**다.

### 아키텍처 (한 줄 흐름)

```
tree-sitter 파싱 → 언어중립 IR → CPG(AST·CFG·def-use·call 4엣지)
  → 프로시저간 taint(함수 요약 기반, IFDS 전체구현 아님) → LLM 트리아지(선택)
  → 감사 작업대(사람 판정) → 산출물(PDF/Word/xlsx/SARIF/CSV)
```

탐지는 **두 축**이다.
- **데이터 흐름(taint)**: 파서가 있는 언어만. 규칙 78종, YAML 스펙.
- **패턴**: 모든 텍스트 파일(.env·.sql·.yml·.properties 포함). 규칙 59종.
  하드코딩 비밀정보·개인정보(주민번호/카드 체크섬 검증기 포함)·설정 위생.

---

## 2. 지금 있는 기능

### 엔진
- 언어 11종: JS/TS(+JSX/TSX) · PHP · Python · Java · Kotlin · Go · Ruby · C · C++ · Swift · C#
- 프로시저간 분석(함수 요약, 고정점 반복으로 재귀·상호재귀 처리), 흐름 민감도(분기 merge, 반복문 고정점)
- 상수 전파(constfold), 컨테이너 오염 전파(list.add/map.put), 리터럴 키 맵 정밀 추적
- sanitizer 인식, 규칙별 sink 사전 필터링(대규모 최적화)
- 파싱 캐시(경로+내용 해시, 정규화기 소스 해시로 자동 무효화)
- 멀티프로세스 파싱(`--jobs`)
- 무결성 보고: "탐지 0건"이 안전인지 못 읽어서인지 구분(실패·부분분석·크기초과 파일 보고)

### 화면 (Django SSR, 11개 템플릿)
- **감사 작업대**(3분할, Fortify/Ghidra 참고): 좌 이슈 탐색기(그룹·검색·필터) /
  중앙 코드 뷰어에 **Source→Sink 흐름 인라인 하이라이트**(단계 클릭 이동) / 우 인스펙터
- 감사 상태 5종: 미확인·취약확정·오탐·조치완료·보류 + 감사자 의견 메모
  → **판정 즉시 화면 카운트가 줄어든다**(조치대상 = 전체 − 오탐·보류·조치완료)
- 하단 도킹 패널: AI 분석(프리셋 7종·자유질문) / 데이터 흐름 세로 시각화 / 로그
- 문법 하이라이팅, 커맨드 팔레트(Ctrl+P), 우클릭 컨텍스트 메뉴, 키보드(j/k, n/p)
- 패널 크기·표시·필터 localStorage 영속
- **프로젝트 홈**: 보안 상태·위험 증감·우선 조사(신규→위험도, 미감사만)·추세·스캔 이력
- 포트폴리오(다중 프로젝트), 스냅샷 비교, 리포트 화면, 설정(LLM 키·보고서 메타)
- 진행 화면(단계 체크리스트·실시간 로그), 배치 진행 화면(프로젝트별 상태 행)
- 스캔 중단, 스캔 이력 다건 선택 삭제, **진행 중 진단으로 돌아가는 상단 띠**
- 테마 4종(다크/라이트/VS Code/Ghidra), 한/영 전환

### 산출물 (이게 국내 시장의 핵심)
- **점검 기준 4종** 매핑: 행정안전부 소프트웨어 개발보안 가이드(48항목) ·
  전자금융감독규정 웹 취약점(28항목) · OWASP Top 10 2021(10) · CWE Top 25(30)
  → 진단 전에 복수 선택하면 산출물이 그 점검표로 나온다
- **합본 진단 결과 보고서**(PDF/Word): 여러 프로젝트를 한 건의 진단으로 묶고
  **최초 검출 → 정오탐 점검 → 최종 조치대상** 구조로 싣는다. 진단원 판정 근거 포함.
- **14컬럼 분석목록표**(xlsx): 고객 제출 규격. 조치여부·조치방법·이슈의견·위험도 음영.
- 유형별 조치 가이드(PDF), SARIF(GitHub Code Scanning), CSV
- 한글 폰트(나눔고딕 OFL) 패키지 동봉 — 폰트 없는 리눅스 서버에서도 한글 산출물이 나온다

### 연동
- GitHub Action(`action.yml`) + SARIF 업로드 + `fail-on` 심각도 게이트
- CI: ubuntu 러너에서 테스트 스위트 실행
- LLM 트리아지 3사(Claude/OpenAI/Gemini), 클러스터링·캐시로 비용 통제

---

## 3. 측정된 성능 (비교 근거)

**OWASP Benchmark v1.2**(Java, 라벨된 정답지). 데이터 흐름 대상 6유형 **N=1,572**
(설정·API 사용 점검류 1,168건은 taint 대상이 아니라 제외, 그 사실도 함께 공개).

| 도구 | 재현율 | 정밀도 | F1 | 오탐률 | 공식 점수(재현율−오탐률) |
|---|---:|---:|---:|---:|---:|
| **CPGuard 0.1.4** | 65.1% | **73.2%** | 0.689 | **25.9%** | **0.392** |
| Semgrep OSS (p/java) | **86.4%** | 58.9% | **0.701** | 65.6% | 0.208 |

같은 정답지·같은 채점 코드(`bench/`)로 측정. Semgrep 은 더 많이 찾고 오탐이 훨씬 많다.
**주의**: Semgrep **Pro**(유료, interfile 분석)는 측정하지 않았다. Semgrep OSS 도
`tainted-*-from-http-request` 같은 taint 규칙을 쓴다 — "쟤들은 정규식"이라는 대비는 틀렸다.
정확한 대비는 **intrafile taint vs 파일을 넘는 interprocedural taint**.

실측 규모: 26,049 파일 / 2.3GB 프로젝트를 약 5분에 4,857건 탐지.

---

## 4. 알려진 한계 (의도적 선택 포함)

- **경로 민감도 없음** — 분기 조건을 해석하지 않는다. `is_numeric()` 로 검증한 뒤 쓰는
  흐름도 취약으로 본다. 벤치마크 오탐의 대부분이 여기서 나온다.
- alias/points-to 분석 없음. 필드 민감도는 경로 문자열 접두 매칭 수준.
- 호출 해석은 **이름 정확 일치만** — import 별칭·동적 디스패치·리플렉션 미해석.
- 프레임워크 인지 없음 — Spring/Django/Express/Laravel 의 라우팅·ORM·템플릿을 모른다.
- 과대근사 정책: 모르는 함수는 인자 오염을 결과로 흘린다, 컨테이너 뮤테이션은 전체 오염.
  (보안 도구에서는 미탐보다 오탐이 낫다는 판단. 주석에 근거까지 기록돼 있음)
- 증분 스캔은 파싱 캐시 수준. 요약 캐시까지는 아직.
- 멀티 유저·RBAC·SSO·감사 로그 없음(현재는 1인 데스크톱 전제).

---

## 5. 아직 없는 것 / 하고 싶은 것

내부 설계 문서(`1.md`/`2.md`)에서 뽑은 미착수 목록 + 실사용에서 나온 것들.

**화면·워크플로**
- 스캔 결과 요약 화면(진단 직후 한 장 요약)
- 두 스캔 선택 비교 화면(현재는 별도 compare 화면만)
- 벌크 액션(다중 선택 후 일괄 판정), 트리아지 사유 선택(오탐 이유 코드화)
- 글로벌 검색(현재 Ctrl+P 는 현재 필터 포커스 수준)
- 리포트 생성 화면(현재는 버튼으로 바로 내보내기)
- 모바일(APK/IPA) 분석

**엔진**
- 경로 민감도 / sanitizer·검증 함수 인식 강화
- 프레임워크 인지(라우팅=source, 템플릿=sink)
- 증분 분석 정식화(요약 캐시), 분산 스캔, CPG 온디스크

**팀·기업**
- 멀티 유저·RBAC·SSO·감사 로그
- 베이스라인/억제 규칙(기존 이슈 제외, 신규만 게이트)
- 이슈 트래커 연동(Jira/GitHub Issues), PR 코멘트
- 서버 에디션(PostgreSQL·Docker/Helm)

---

## 6. 제약 (제안할 때 반드시 지킬 것)

1. **1인 개발.** 반년 내 혼자 만들 수 있는 크기여야 한다. "ML 기반 재랭킹" 같은 건
   아이디어로만 유용하고 실행 계획으로는 무용하다.
2. **오프라인 우선.** 에어갭 고객이 주 대상이라 클라우드 필수 기능은 곤란하다.
3. **엔진 정면승부는 안 한다.** Semgrep/CodeQL 과 엔진 품질로 겨루는 건 승산이 없다.
   차별점은 **한국 컴플라이언스 산출물 · 오프라인 · 진단원 워크플로** 세 가지다.
4. **IP 경계.** SPARROW 의 UI·코드·룰팩·문안을 복제하지 않는다. 참고하는 것은
   **"어떤 기능이 왜 필요한가"** 라는 문제 정의까지다. 화면 배치나 표현을 베끼지 말 것.
5. 스택은 Python/Django/tree-sitter 로 고정. 새 런타임 도입은 제안에서 제외.

---

## 7. 요청하는 산출물 (이 형식으로 답해 줘)

SPARROW 5.6 을 브라우저로 확인하고, **CPGuard 에 붙일 가치가 있는 기능**을 아래 표로.

| # | 기능명 | SPARROW 에서 하는 일 | CPGuard 현황 | 왜 필요한가 | 구현 난이도 | 붙일 지점 |
|---|---|---|---|---|---|---|
| 1 | (예) 이슈 억제 규칙 | 파일·규칙 단위 제외를 프로젝트 설정으로 저장 | 없음 | 재스캔마다 같은 오탐을 다시 판정 | 중 | `web/models.py` + 작업대 필터 |

- **CPGuard 현황** 은 위 2절을 근거로 판단해서 `있음 / 부분 / 없음` 중 하나로.
  "부분"이면 뭐가 모자란지 한 줄.
- **구현 난이도**: 하(며칠) / 중(1~2주) / 상(한 달+). 6절 제약을 반영해서.
- **붙일 지점**: 우리 구조상 어디를 건드려야 하는지. 모르면 비워도 된다.
- 마지막에 **우선순위 5개**를 골라 이유 한 줄씩. 우선순위 기준은
  "국내 진단 실무에서 즉시 값이 나오는가" > "팀 도입의 관문인가" > "엔진 정확도".
- 이미 우리가 가진 기능은 표에서 빼거나 `있음` 으로 명시. 중복 제안이 제일 쓸모없다.

추가로 알려주면 좋은 것:
- SPARROW 가 **산출물(보고서·점검표)** 를 어떻게 내는지. 우리 14컬럼 분석목록표·합본
  보고서와 비교해서 빠진 항목이 있는지.
- 진단원 판정 워크플로(정오탐 처리)가 우리와 어떻게 다른지.
- 우리가 **하지 말아야 할 것**이 보이면 그것도. (과설계로 보이는 기능)
